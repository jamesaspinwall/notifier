<%= form_for(email_reminder) do |f| %>
  <% if email_reminder.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(email_reminder.errors.count, "error") %> prohibited this email_reminder from being saved:</h2>

      <ul>
      <% email_reminder.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :chronic %>
    <%= f.text_field :chronic %>
  </div>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
